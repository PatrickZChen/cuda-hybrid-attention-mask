cmake_minimum_required(VERSION 3.24)

project(cuda_hybrid_attention_mask LANGUAGES NONE)

# Milestone 1 contains only the PyTorch reference and its tests. Future
# milestones will enable C++/CUDA and add concrete build targets.
